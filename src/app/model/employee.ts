export interface Employee {
    empId?: number;
    name?: string; // ? - optional - could be asssigned with a value or undefined 
    salary?: number;
    photo?: string;
    status?: boolean      
}